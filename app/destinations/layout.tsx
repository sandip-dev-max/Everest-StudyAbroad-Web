import type { Metadata } from 'next';
export const metadata: Metadata = { title:'Study Destinations', description:'Compare international study destinations, costs, universities and pathways with Everest Global Network.' };
export default function Layout({children}:{children:React.ReactNode}){return children}
