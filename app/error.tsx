'use client';
import { useEffect } from 'react';
import Link from 'next/link';
export default function Error({ error, reset }: { error: Error & { digest?: string }; reset: () => void }) { useEffect(()=>{ console.error(error); },[error]); return <main className="min-h-[70vh] px-5 pb-24 pt-40"><div className="mx-auto max-w-3xl"><p className="eyebrow">Something went wrong</p><h1 className="mt-4 text-5xl font-semibold tracking-[-.05em] sm:text-7xl">Let's try that <span className="font-serif font-normal italic text-slate-500">again.</span></h1><p className="mt-5 text-base leading-7 text-slate-500">We couldn't load this page. Your information and existing data are safe.</p><div className="mt-8 flex flex-wrap gap-3"><button onClick={()=>reset()} className="rounded-full bg-[#087FC1] px-6 py-3.5 text-sm font-bold text-white">Try again</button><Link href="/" className="rounded-full border border-slate-200 bg-white px-6 py-3.5 text-sm font-bold">Go home</Link></div></div></main>}
