import type { Metadata } from 'next';
export const metadata: Metadata = { title:'Study Abroad Services', description:'Explore Everest Global Network services for admissions, documentation, visa preparation, scholarships, test preparation and pre-departure support.' };
export default function Layout({children}:{children:React.ReactNode}){return children}
