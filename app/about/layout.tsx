import type { Metadata } from 'next';
export const metadata: Metadata = { title:'About Everest Global Network', description:'Meet Everest Global Network, our counsellors, mission, values and approach to helping students from Nepal study abroad.' };
export default function Layout({children}:{children:React.ReactNode}){return children}
