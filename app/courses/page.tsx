import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import { CoursesClient } from "@/components/CoursesClient";
import { courses, faculties } from "@/data/catalog";
export default function CoursesPage(){return <main id="main-content" className="pt-28"><section className="bg-[#F5F7FB] py-20 lg:py-28"><div className="mx-auto max-w-[1380px] px-5 sm:px-8 lg:px-10"><div className="grid gap-10 lg:grid-cols-[1fr_.6fr] lg:items-end"><div><p className="eyebrow">Course catalogue</p><h1 className="mt-4 max-w-5xl text-6xl font-semibold leading-[.9] tracking-[-.07em] sm:text-8xl">Find your <span className="font-serif font-normal italic text-[#087FC1]">next subject.</span></h1></div><p className="max-w-xl text-base leading-7 text-slate-500">Search {courses.length}+ programmes from the existing Everest catalogue and compare by subject, level and destination.</p></div></div></section><section className="bg-white py-12 lg:py-16"><div className="mx-auto max-w-[1380px] px-5 sm:px-8 lg:px-10"><div className="mb-8 flex flex-wrap items-center gap-2"><span className="rounded-full bg-[#0B1B3A] px-4 py-2 text-[10px] font-bold uppercase tracking-[.16em] text-white">Most searched</span>{faculties.slice(0,8).map(f=><Link key={f.slug} href={`/courses?faculty=${f.slug}`} className="rounded-full border border-slate-200 px-4 py-2 text-[10px] font-bold uppercase tracking-[.16em] text-slate-500 transition hover:border-[#087FC1] hover:text-[#087FC1]">{f.name}</Link>)}</div><CoursesClient/></div></section></main>}
