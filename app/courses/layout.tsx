import type { Metadata } from 'next';
export const metadata: Metadata = { title:'Courses', description:'Search courses by subject, study level, destination and university with Everest Global Network.' };
export default function Layout({children}:{children:React.ReactNode}){return children}
